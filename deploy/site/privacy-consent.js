'use strict';
// Compatibility stub retained so older cached HTML does not fail while the new landing page propagates.
// Access to downloads and the web client is no longer blocked by a consent checkbox.
