{
  "name": "galactic-rpg-mobile",
  "private": true,
  "version": "1.0.143",
  "description": "Mobile player client for Galactic RPG Interface",
  "scripts": {
    "android:init": "npx cap add android",
    "sync": "npx cap sync android",
    "copy": "npx cap copy android",
    "open:android": "npx cap open android",
    "prepare:web": "node ../tools/prepare-mobile.cjs",
    "presync": "npm run prepare:web",
    "precopy": "npm run prepare:web"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@supabase/supabase-js": "^2.57.4",
    "@capacitor/preferences": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.9.2"
  }
}
